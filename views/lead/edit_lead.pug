extends ../layout

block content
  h1 Update
    form(action="/lead/" + lead.id + "/edit", method="POST")
      input(type="email", name="lead_email",placeholder="Enter your E-mail", value=lead.email)
      button(type="submit") Save